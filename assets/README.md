# Figure assets

The figure referenced by the top-level `README.md` is in place:

| File | Paper figure | Content |
| --- | --- | --- |
| `figure1_framework.png` | Figure 1 | Framework overview (profile synthesis → SFT → test-time multi-perspective reasoning) |

`figure1_framework.png` was rendered from the paper's arXiv source (`figure1_framework.src.pdf`, kept here as the
vector source). To regenerate, use any PDF→PNG tool (e.g. `pdftoppm -png -r 300 figure1_framework.src.pdf figure1`).
